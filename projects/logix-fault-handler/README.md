# Logix first-out fault handler

**Status:** files not in repo yet  
**Platform:** Studio 5000  
**Why it is here:** the pattern, not the whole class project.

Capture:

- fault UDT (code, source, timestamp, latch)
- first-out vs subsequent
- reset rules (who is allowed to clear what)
- RTO vs TON choice for the faults that need it

Sanitize any lab naming that points at a specific employer or plant.
