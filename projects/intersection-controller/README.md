# Intersection / traffic controller

**Status:** files not in repo yet  
**Platform:** Studio 5000, Factory I/O or SoftLogix  
**Why it is here:** state machine + conflicting-call interlocks. Same thinking as a machine cell.

Publish only if the write-up covers:

- states (through, left, pedestrian, all-red, flash)
- interlocks that prevent green-vs-green
- fault / flash fallback and reset

A bare 4-way-stop lab worksheet does not belong in public form.
